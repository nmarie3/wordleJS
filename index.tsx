#!/usr/bin/env node
import {checkGuess} from './wordleTS';
import chalk from 'chalk';

function playGame() {
    const word = 'enemy';


    if (guess === word);
    console.log(chalk.green('You got it!'));
}